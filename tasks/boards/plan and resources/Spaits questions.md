1. In Next.js can you set one component (the add to list form) to 'use client' and have it affect another component (the grocery list) which is using server rendering? If so, how do I do that?
2. Does Next have a system for calling back to the server and changing the server rendering?  Does State work for that?

