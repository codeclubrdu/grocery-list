# Welcome to Tasks.md

This card is a plain markdown file at `tasks/boards/backlog/`. Lanes are directories, cards are `.md` files — everything is versioned with git.

- [ ] Run `pnpm tasks` and open <http://localhost:8338>
- [ ] Drag this card around
- [ ] Delete it when you're done